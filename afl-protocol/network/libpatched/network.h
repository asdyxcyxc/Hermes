#ifndef NETWORK_H
#define NETWORK_H

#include <stdio.h>
#include <unistd.h>
#include <sys/syscall.h>
#include "../../config.h"
#include <errno.h>
#include <string.h>

// int socket(int, int, int);
// ssize_t recv(int , void *, size_t, int);

#endif /* NETWORK_H */
